# 16: Удаление прототипа

**What to build:** Прототипные данные и скрипты уходят из репозитория. Ничего их больше не читает.

**Blocked by:** 05, 10

**Status:** ready-for-agent

- [ ] `data/plan.json` удалён
- [ ] `static/data/days.jsonl` и `static/data/exercises.jsonl` удалены
- [ ] `tools/build_data.py` удалён после того, как конвертация выполнена
- [ ] `data/procedures.json`, `data/links.json` и `data/dose_by_key.json` удалены после переноса в таблицы
- [ ] Роут `/dev` удалён или переведён на таблицы
- [ ] Критики в `tools/` перенацелены на таблицы, правила предметной области сохранены
- [ ] `make check` проходит на новых данных
- [ ] Поиск по репозиторию не находит ни одной ссылки на удалённые файлы
